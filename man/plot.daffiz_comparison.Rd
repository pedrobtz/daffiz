% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.daffiz_comparison}
\alias{plot.daffiz_comparison}
\title{Plot method for a comparison}
\usage{
\method{plot}{daffiz_comparison}(x, y, ...)
}
\arguments{
\item{x}{A \code{daffiz_comparison} from \code{\link[=compare_dt]{compare_dt()}}.}

\item{y}{Unused; present for compatibility with the \code{\link[=plot]{plot()}} generic.
Supplying it is an error rather than a silent no-op, because the sibling
functions take \code{columns} in that position.}

\item{...}{Passed to \code{\link[=plot_diff]{plot_diff()}}.}
}
\value{
The plot object, invisibly.
}
\description{
Equivalent to building \code{\link[=plot_diff]{plot_diff()}} and printing it, so that the
conventional \code{plot(cmp)} call draws the difference map instead of falling
through to \code{plot.default()}.
}
\examples{
x <- data.frame(id = 1:3, amount = c(10, 20, 30))
y <- data.frame(id = 1:3, amount = c(10, 25, 27))
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot(compare_dt(x, y))
}
}
