% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors-rows.R
\name{original_rows}
\alias{original_rows}
\title{Recover rows from the original input snapshots}
\usage{
original_rows(
  cmp,
  side = c("x", "y"),
  type = c("affected", "changed", "only", "all")
)
}
\arguments{
\item{cmp}{A \code{daffiz_comparison} from \code{\link[=compare_dt]{compare_dt()}}.}

\item{side}{Either \code{"x"} or \code{"y"}.}

\item{type}{Which rows to recover: all affected rows, matched changed rows,
side-only rows, or all original rows.}
}
\value{
A \code{data.table} copied from the stored input snapshot.
}
\description{
The returned rows preserve the original column classes and values, and add
the appropriate \code{.row_x} or \code{.row_y} traceability column.
}
\details{
Column \emph{order} follows \code{x} on both sides: preflight aligns \code{y} to the
validated column order of \code{x} (design plan section 4.1, gate 4), so the two
sides stay directly comparable. For \code{side = "x"} that is also the input's
own order.
}
\examples{
x <- data.frame(id = 1:2, amount = c(10, 20))
y <- data.frame(id = 2:3, amount = c(20, 30))
cmp <- compare_dt(x, y)
original_rows(cmp, "x")
original_rows(cmp, "y", type = "only")
}
