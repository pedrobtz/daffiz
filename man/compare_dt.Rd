% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare-dt.R
\name{compare_dt}
\alias{compare_dt}
\title{Compare the numeric measures of two tables}
\usage{
compare_dt(
  x,
  y,
  by = NULL,
  compare = NULL,
  exclude = NULL,
  abs_tol = 0,
  rel_tol = 0,
  duplicate_keys = c("pair", "report", "error"),
  x_name = NULL,
  y_name = NULL,
  batch = NULL
)
}
\arguments{
\item{x, y}{Tables to compare. \code{data.frame}, \code{data.table} and tibble inputs
are accepted; neither input is modified.}

\item{by}{Identity columns used to align rows. \code{NULL} (default) infers every
column that is not a default measure. Pass \code{\link[=daffiz_row_number]{daffiz_row_number()}} to align
by position instead.}

\item{compare}{Numeric measures to compare. \code{NULL} (default) infers every
column that is an unclassed double in both inputs and is not claimed by
\code{by}.}

\item{exclude}{Columns dropped before roles are resolved.}

\item{abs_tol, rel_tol}{Absolute and relative tolerance. Either a single
value applied to every measure, or a named vector using \code{.default} for
unspecified columns, for example \code{c(.default = 0, amount = 0.01)}.}

\item{duplicate_keys}{What to do when an identity is duplicated. \code{"pair"}
sorts deterministically and compares paired rows; \code{"report"} reports the
ambiguous groups and excludes them from cell comparison; \code{"error"} stops.}

\item{x_name, y_name}{Labels used in reports. Default to the argument
expressions when those are bare symbols, otherwise \code{"x"} and \code{"y"}.}

\item{batch}{Maximum number of measures melted and joined at once. \code{NULL}
(default) processes every measure in one batch. Smaller values can reduce
peak intermediate memory without changing or discarding result cells.}
}
\value{
A \code{daffiz_comparison} object. See \code{\link[=all_cells]{all_cells()}}.
}
\description{
Compares two tables that are expected to be almost the same, and reports
which numeric measures and which source rows differ. Rows are aligned on
identity columns; unclassed double columns are compared as measures.
}
\details{
By default every column that is an unclassed double in \emph{both} inputs is a
measure, and every other column forms the row identity. \code{Date} and \code{POSIXct}
columns are doubles internally but are \emph{not} measures by default; they
remain identity columns. Use \verb{by=} and \verb{compare=} to override either role,
and \code{by = daffiz_row_number()} to align rows by position.

Reading both inputs keeps role inference symmetric. A column that is integer
on one side and double on the other belongs to neither default role, so it
is reported as an identity type mismatch in either argument order; name it
in \verb{compare=} to compare it as a measure.

A value matches when
\code{abs(x - y) <= max(abs_tol, rel_tol * max(abs(x), abs(y)))}. The rule is
symmetric, so swapping the inputs negates the finite differences without
changing any match decision.

The result holds one row per aligned numeric cell, so it has
\verb{nrow x length(compare)} rows. A warning is issued above
\code{getOption("daffiz.max_cells")}, which defaults to 1e7.
}
\examples{
x <- data.frame(id = 1:3, amount = c(1, 2, 3))
y <- data.frame(id = 1:3, amount = c(1, 2, 3.5))
cmp <- compare_dt(x, y)
is_matching(cmp)
all_cells(cmp)
}
