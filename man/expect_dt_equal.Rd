% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expectations.R
\name{expect_dt_equal}
\alias{expect_dt_equal}
\title{Expect two tables to match within tolerance}
\usage{
expect_dt_equal(
  object,
  expected,
  ...,
  max_columns = 10L,
  max_rows = 10L,
  max_cells = 20L
)
}
\arguments{
\item{object, expected}{Tables to compare.}

\item{...}{Arguments passed to \code{\link[=compare_dt]{compare_dt()}}, for example \code{by}, \code{compare},
\code{abs_tol}, \code{rel_tol} or \code{duplicate_keys}.}

\item{max_columns, max_rows, max_cells}{Maximum diagnostic records included in
a failure message.}
}
\value{
The comparison object, invisibly.
}
\description{
A testthat expectation backed by \code{\link[=compare_dt]{compare_dt()}}. Failures contain bounded,
deterministic column, row, and cell diagnostics. The complete comparison is
attached to the failure condition as its \code{comparison} attribute.
}
\details{
Unlike \code{\link[=compare_dt]{compare_dt()}}, this defaults to \code{duplicate_keys = "error"}: a
duplicated identity makes row alignment a heuristic, which is not a sound
basis for a passing assertion. Pass \code{duplicate_keys = "pair"} explicitly to
accept the heuristic.
}
\examples{
x <- data.frame(id = 1:2, amount = c(10, 20))
expect_dt_equal(x, x)
}
