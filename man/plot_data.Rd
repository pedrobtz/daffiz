% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_data}
\alias{plot_data}
\title{Data behind the difference map}
\usage{
plot_data(cmp, columns = NULL, max_rows = 500L, row_bins = 100L)
}
\arguments{
\item{cmp}{A \code{daffiz_comparison} from \code{\link[=compare_dt]{compare_dt()}}.}

\item{columns}{Optional character vector of compared measures to include.
Defaults to every compared measure, in input-column order.}

\item{max_rows}{Largest number of records drawn one tile per row. Above
this, rows are binned. \code{NULL} or \code{Inf} never bins.}

\item{row_bins}{Maximum number of row bins used when binning applies.}
}
\value{
A \code{data.table} with one row per tile.
}
\description{
Returns the exact table \code{\link[=plot_diff]{plot_diff()}} draws, without requiring \code{ggplot2}.
This makes row ordering, binning, and the match classification of every tile
visible and testable.
}
\details{
There is one record per tile. Rows appear in original source position order,
never ranked, so that regressions concentrated in one region of the input
remain visible as a band. When the comparison has more than \code{max_rows}
aligned or side-only records, contiguous rows are aggregated into at most
\code{row_bins} bins and \code{binned} is \code{TRUE}; every eligible row is represented
either way, and no row is ever discarded.

\code{fraction_mismatched} is the share of cells behind the tile that are not
\code{equal}. Unbinned tiles cover one cell, so it is \code{0} or \code{1} there, and
\code{match_type}, \code{symbol} and the source values are populated. Binned tiles
cover several cells, so those per-cell columns are \code{NA}. \code{row_start} and
\code{row_end} give the tile's extent in display order either way.
}
\examples{
x <- data.frame(id = 1:3, amount = c(10, 20, 30))
y <- data.frame(id = 1:3, amount = c(10, 25, 27))
cmp <- compare_dt(x, y)
plot_data(cmp)
}
